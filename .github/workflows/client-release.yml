name: Build Windows and Android Release

run-name: Client release from ${{ github.ref_name }} by ${{ github.actor }}

on:
    workflow_dispatch:
        inputs:
            release_tag:
                description: "Optional release tag (leave empty to generate one automatically)"
                required: false
                type: string

permissions:
    contents: write

concurrency:
    group: client-release-${{ github.run_id }}
    cancel-in-progress: false

jobs:
    prepare-release:
        name: Prepare draft release
        runs-on: ubuntu-latest
        outputs:
            tag: ${{ steps.metadata.outputs.tag }}
            version: ${{ steps.metadata.outputs.version }}

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Resolve release metadata
              id: metadata
              shell: bash
              env:
                  INPUT_TAG: ${{ inputs.release_tag }}
              run: |
                  VERSION="$(jq -r '.version' apps/core/package.json)"
                  TAG="$INPUT_TAG"
                  if [[ -z "$TAG" ]]; then
                    TAG="v${VERSION}-build.${GITHUB_RUN_NUMBER}"
                  fi

                  if [[ ! "$TAG" =~ ^v[0-9A-Za-z][0-9A-Za-z._-]*$ ]]; then
                    echo "Invalid release tag: $TAG" >&2
                    echo "Use a tag such as v1.11.4.1-custom.1." >&2
                    exit 1
                  fi

                  echo "tag=$TAG" >> "$GITHUB_OUTPUT"
                  echo "version=$VERSION" >> "$GITHUB_OUTPUT"

            - name: Create draft release
              shell: bash
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ steps.metadata.outputs.tag }}
                  VERSION: ${{ steps.metadata.outputs.version }}
              run: |
                  if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
                    echo "Release or tag $TAG already exists. Choose another tag." >&2
                    exit 1
                  fi

                  gh release create "$TAG" \
                    --repo "$GITHUB_REPOSITORY" \
                    --target "$GITHUB_SHA" \
                    --title "无名杀 ${TAG}" \
                    --notes "基于 ${GITHUB_SHA} 构建的无名杀 ${VERSION} 分发包。包含 Windows NSIS 安装器、使用测试签名的 Android release APK、独立大厅 Linux AMD64 Docker 镜像 tar，以及解压即用的跨平台 Node.js 大厅 zip。" \
                    --draft

    build-windows:
        name: Build Windows EXE
        needs: prepare-release
        runs-on: windows-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 9
                  run_install: false

            - name: Install Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm

            - name: Install dependencies
              run: pnpm install --no-frozen-lockfile

            - name: Build Windows installer
              shell: bash
              env:
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  pnpm build
                  pnpm -F @noname/electron build:win

                  EXE="$(find output -maxdepth 1 -type f -name '*.exe' -print -quit)"
                  if [[ -z "$EXE" ]]; then
                    echo "Windows installer was not created." >&2
                    exit 1
                  fi

                  mkdir -p release
                  cp "$EXE" "release/noname-windows-${TAG#v}.exe"

            - name: Upload Windows installer
              shell: bash
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  gh release upload "$TAG" \
                    "release/noname-windows-${TAG#v}.exe" \
                    --repo "$GITHUB_REPOSITORY" \
                    --clobber

    build-android:
        name: Build Android APK
        needs: prepare-release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 9
                  run_install: false

            - name: Install Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm

            - name: Install Java
              uses: actions/setup-java@v5
              with:
                  distribution: temurin
                  java-version: "21"
                  cache: gradle

            - name: Install Android SDK
              uses: android-actions/setup-android@v3

            - name: Install Android API 36
              shell: bash
              run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

            - name: Install dependencies
              run: pnpm install --no-frozen-lockfile

            - name: Build Android release APK
              shell: bash
              env:
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  pnpm --dir apps/mobile build:apk

                  APK="apps/mobile/android/app/build/outputs/apk/release/app-release.apk"
                  if [[ ! -f "$APK" ]]; then
                    echo "Android APK was not created." >&2
                    exit 1
                  fi

                  mkdir -p release
                  cp "$APK" "release/noname-android-${TAG#v}.apk"

            - name: Upload Android APK
              shell: bash
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  gh release upload "$TAG" \
                    "release/noname-android-${TAG#v}.apk" \
                    --repo "$GITHUB_REPOSITORY" \
                    --clobber

    build-lobby-packages:
        name: Build lobby Docker and Node packages
        needs: prepare-release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 9
                  run_install: false

            - name: Install Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm

            - name: Install dependencies
              run: pnpm install --no-frozen-lockfile

            - name: Build portable Node.js package
              shell: bash
              env:
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  PACKAGE_DIR="$RUNNER_TEMP/noname-lobby-node"
                  LINKED_MODULES="$RUNNER_TEMP/noname-lobby-linked-modules"

                  pnpm --dir packages/server build
                  pnpm --filter @noname/server deploy --prod "$PACKAGE_DIR"

                  mv "$PACKAGE_DIR/node_modules" "$LINKED_MODULES"
                  mkdir "$PACKAGE_DIR/node_modules"
                  cp -RL "$LINKED_MODULES/." "$PACKAGE_DIR/node_modules/"

                  if find "$PACKAGE_DIR" -type l -print -quit | grep -q .; then
                    echo "Portable Node.js package still contains symbolic links." >&2
                    exit 1
                  fi

                  node --check "$PACKAGE_DIR/dist/cli.cjs"
                  mkdir -p release
                  (
                    cd "$PACKAGE_DIR"
                    7z a -tzip "$GITHUB_WORKSPACE/release/noname-lobby-node-${TAG#v}.zip" .
                  )
                  7z t "release/noname-lobby-node-${TAG#v}.zip"

            - name: Build Docker image tar
              shell: bash
              env:
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  IMAGE="noname-lobby:${TAG#v}"
                  TAR="release/noname-lobby-docker-linux-amd64-${TAG#v}.tar"

                  docker build --platform linux/amd64 --tag "$IMAGE" packages/server
                  docker image inspect "$IMAGE"
                  docker save --output "$TAR" "$IMAGE"
                  tar -tf "$TAR" >/dev/null

            - name: Upload lobby packages
              shell: bash
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  gh release upload "$TAG" \
                    "release/noname-lobby-docker-linux-amd64-${TAG#v}.tar" \
                    "release/noname-lobby-node-${TAG#v}.zip" \
                    --repo "$GITHUB_REPOSITORY" \
                    --clobber

    publish-release:
        name: Publish release
        needs:
            - prepare-release
            - build-windows
            - build-android
            - build-lobby-packages
        runs-on: ubuntu-latest

        steps:
            - name: Publish GitHub release
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  gh release edit "$TAG" \
                    --repo "$GITHUB_REPOSITORY" \
                    --draft=false \
                    --latest

    cleanup-failed-release:
        name: Clean up failed release
        if: >-
            ${{
              always() &&
              needs.prepare-release.result == 'success' &&
              (
                needs.build-windows.result != 'success' ||
                needs.build-android.result != 'success' ||
                needs.build-lobby-packages.result != 'success'
              )
            }}
        needs:
            - prepare-release
            - build-windows
            - build-android
            - build-lobby-packages
        runs-on: ubuntu-latest

        steps:
            - name: Delete failed draft release and tag
              env:
                  GH_TOKEN: ${{ github.token }}
                  TAG: ${{ needs.prepare-release.outputs.tag }}
              run: |
                  gh release delete "$TAG" \
                    --repo "$GITHUB_REPOSITORY" \
                    --yes
